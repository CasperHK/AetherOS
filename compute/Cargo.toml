[package]
name        = "aether-compute"
version.workspace   = true
edition.workspace   = true
authors.workspace   = true
license.workspace   = true
description = "AetherOS Mojo AI compute-engine bridge (placeholder)"

[dependencies]
aether-common = { path = "../common" }
